{
  "name": "@mirai/voice-agent-container",
  "version": "1.0.0",
  "description": "Cloudflare Container for Inworld Runtime Voice Agent",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy:local": "wrangler dev --env dev --port 4000 --name voice-agent-container",
    "deploy:cf": "wrangler deploy",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test:docker": "docker build --target development -t voice-agent-dev . && docker run -p 4000:4000 --env-file .env --rm voice-agent-dev",
    "test:local": "pnpm run dev",
    "test": "vitest",
    "format": "prettier --write .",
    "lint": "eslint ."
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.28"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250110.0",
    "@types/node": "^22.14.0",
    "esbuild": "^0.24.2",
    "prettier": "^3.4.2",
    "typescript": "^5.8.3",
    "vitest": "^3.0.5",
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
