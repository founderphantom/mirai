# Production Environment Configuration

# API Gateway URL (via unified domain with service binding)
VITE_API_URL=https://miraichat.app

# WebSocket URL (via unified domain with service binding)
VITE_WS_URL=wss://miraichat.app

# Legacy SDK WebSocket (disabled - not used in current architecture)
VITE_AIRI_WS_URL=

# Environment
VITE_ENVIRONMENT=production
